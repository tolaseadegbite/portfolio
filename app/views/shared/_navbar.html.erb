<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "LASHE", root_path, class: "navbar-brand fw-bolder p-3 border text-decoration-none btn btn-light" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-auto">
        <%= link_to "Home", root_path, class: "nav-link text-dark me-2" %>

        <%= link_to "Projects", projects_path, class: "nav-link text-dark me-2" %>

        <%#= link_to "Resume", '#', class: "nav-link text-dark me-2" %>

        <%= link_to "Blog", posts_path, class: "nav-link text-dark me-2" %>

        <%= link_to "Contact", '#', class: "nav-link text-dark" %>

      </ul>

        <% if user_signed_in? %>
            <ul class="navbar-nav mb-lg-2 mb-lg">
              <li class="nav-item dropdown">
                <a class="nav-link text-dark" style="min-width:150px; text-align:right" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <%= gravatar_for current_user, size: '40px' %>
                  <% if !current_user.username.blank? %>
                    <%= current_user.username %>
                  <% else %>
                    <%= current_user.email %>
                  <% end %>
                  <%= image_tag 'menu.png', width: "22" %>
                </a>
                <ul class="dropdown-menu rounded-0 p-0">
                    <%= link_to "Account settings", edit_user_registration_path, class: "dropdown-item border-bottom" %>
                    <%= link_to "Profile", profile_path(current_user.username), class: "dropdown-item border-bottom" %>
                    <%= link_to "Saved posts", '#', class: "dropdown-item border-bottom" %>
                    <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item text-danger" %>
                </ul>
              </li>
            </ul>
        <% else %>
            <ul class="navbar-nav">
                <%= link_to "Log in", new_user_session_path, class: "nav-link text-dark mr-1" %>
                <%= link_to "Sign up", new_user_registration_path, class: "nav-link text-dark ml-1" %>
            </ul>
        <% end %>
    </div>
  </div>
</nav>